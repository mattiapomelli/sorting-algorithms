#ifndef ARRAY_H
#define ARRAY_H

void printArray( int a[], int n );

void swap( int h[], int i, int j);

/*
 n: address variable to save size of the array into
 returns: pointer to the first element of allocated array
*/
int *scanArray(int *n);

#endif